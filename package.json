{
  "name": "smriti",
  "version": "1.2.0",
  "description": "Personal Claude Code skill stack — one /begin flow from idea to reviewed, tested change.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "gen:skill-docs": "bun run scripts/gen-skill-docs.ts",
    "skill:check": "bun run scripts/skill-check.ts",
    "test": "scripts/run-tests.sh"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "dependencies": {
    "playwright": "1.59.1"
  }
}
